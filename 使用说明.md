export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
claude --dangerously-skip-permissions

=========================================
🔥 如何解决中断退出后，继续当前进度？🔥
Claude Code 原生支持会话记忆。如果你不小心关闭了终端、或者想明天继续开发：
**千万不要**直接输入 `claude` 重新开始！
请使用续传命令：
`claude --continue` (或者缩写 `claude -c`)
这会自动读取你上一次退出时的上下文、记忆和进度，并继续接下来的开发工作。
=========================================
APP DIRECTION: [在这里填入你想做的方向，或留空让AI自己想]
接着，如果你是**全新开发**，输入以下指令触发总控 Skill：
/auto-app-architect

APP DIRECTION: []

Execute your full protocol. Think hard, do not be lazy.

PHASE 0: STATE ASSESSMENT (CRITICAL)
1. Read `SPEC.md`. If it already has an architecture and feature list, DO NOT overwrite it. Determine what is missing in the `lib/` folder and instruct the engineers to continue coding based on the `SPEC.md`.
2. If the project is totally empty, delete `android`, `linux`, `macos`, `web`, and `windows` folders, and start from Phase 1.



CRITICAL REQUIREMENTS: 
- TWO-ROLE DESIGN: The Planner MUST write the idea using the [Hyper-niche hobby] + [Specific input] + [AI output] + [Local archive] formula. The Architect MUST write a detailed multi-file structure into `SPEC.md` BEFORE any coding begins. 
- NO MONOLITHIC CODE: Everything cannot be in `main.dart`. Use `lib/screens/`, `lib/widgets/`, etc.
- STRICT NAMING & UI: The app name MUST be a unique, pleasant portmanteau of 5+ roots (under 15 chars, NO complete words, NO finance/crypto links). The UI MUST feature a randomly generated primary HEX color and a deliberate, diverse premium aesthetic (not default Flutter white). Implement deep interactions (like swipeable cards or peek views) if applicable.
- Tech constraints: `setState` only, `shared_preferences`, English only, safe domains.
- Flow: Splash -> Mock Login -> Bottom Nav -> Settings (with 5 sub-pages). Image Gallery + AI Chat Persona + Stats/History included.



Loop QA and AppStore Reviewer until "STATUS: APPROVED_BY_AI". Only notify me when fully passed.


---
请你根据AGENT_OPTIMIZATIONS.md遇到的问题,补充和更新:.claude/agents、.claude/skills这些路径下的skill和agent设定或工作流
